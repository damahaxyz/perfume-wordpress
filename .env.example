COMPOSE_PROJECT_NAME=wordpress

# 對外監聽設定；若前方另有反向代理，可改成 127.0.0.1。
HTTP_BIND_IP=0.0.0.0
HTTP_PORT=8080

# 請使用不同的高強度密碼，不要保留範例值。
MARIADB_DATABASE=wordpress
MARIADB_USER=wordpress
MARIADB_PASSWORD=replace_with_a_long_random_password
MARIADB_ROOT_PASSWORD=replace_with_another_long_random_password

WORDPRESS_TABLE_PREFIX=wp_

# 固定主要版本，升級前請先備份並查看映像版本說明。
WORDPRESS_IMAGE=wordpress:7.0-php8.3-fpm
MARIADB_IMAGE=mariadb:11.8
NGINX_IMAGE=nginx:1.28-alpine
